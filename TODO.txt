FILE-SPECIFIC BUG FIXES/TODO:
- in config.py: ensure that error messages are consistent (e.g. ", got ..." and "in config.toml")
- in writer.py: consider moving _progress_line() and the time reformatting function to a diff file as that isn't directly related to writing data
- in runner.py: re-review _install_signal_handlers(), make new system for elapsed time

RENAMING:
- connect.py --> connections.py
- core --> src
- ingest --> collect
- preflight --> precollection
- completion --> promotion; complete --> promote

GENERAL TODO:
- write better/more indepth file descs for config.py, connect.py, and listener.py
- modify pyproject.toml 
- add to parquet file metadeta which node provider represents which column #. This is to account for when people switch the order of node providers (you don't want to mislabel the node providers in the chart), run time end (ns)

UPCOMING FEATURES:
- time-based run (e.g. 1 hr run)
- implement data cleaning tool
- implement data analysis tool
    - ensure that charts are labeled based on parquet file metadeta 