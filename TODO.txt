FILE-SPECIFIC BUG FIXES/TODO:
- in runner.py: re-review _install_signal_handlers()
- in cli.py (build parser func) & in the repo desc: update grammar b/c it sounds like the tool analyzes the data itself rather than the timestamps of when it was read

RENAMING:
- prep -> smt more related to dataframes. keep in mind the first func isn't dataframe specific tho

GENERAL TODO:
- rankings charts:
    - add rankings chart based on transactions where EVERY node reported ("common transactions")
    - add quantities based chart for provider rankings/placements as an output to the analysis. would need to split build_place_share() into 2 different functions for this
- modify pyproject.toml 
