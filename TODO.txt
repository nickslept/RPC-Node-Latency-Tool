FILE-SPECIFIC BUG FIXES/TODO:
- in writer.py: consider moving _progress_line() and the time reformatting function to a diff file as that isn't directly related to writing data
- in runner.py: re-review _install_signal_handlers()
- in cli.py & in the repo desc: update grammar b/c it sounds like the tool analyzes the data itself rather than the timestamps of when it was read

RENAMING:
- connect.py --> connections.py
- core --> src
- ingest --> collect
- preflight/pre-flight .. double check for flight --> precollection
- completion --> promotion; complete --> promote

GENERAL TODO:
- modify pyproject.toml 

UPCOMING FEATURES:
- implement data cleaning tool
- implement data analysis tool
    - ensure that charts are labeled based on parquet file metadeta 